class Grandfather
{
	int WALK := 8;
	string RUN := "RUN";
}

class Father extends Grandfather
{
	Father g := nil;
}

class Son extends Father
{
	Son swim;
	int iii;
}

array WisePeople = Grandfather[];

void main()
{
    PrintInt(5);
	Father g      := new Father;
	PrintInt(4);
	g.g           := new Son;
	PrintInt(5);
	WisePeople wp := new Grandfather[180];

	wp[49] := new Son;

    PrintInt(wp[49].WALK);

    PrintInt(wp[49].RUN = "RUN");
}
